import { ApphighDirective } from './apphigh.directive';

describe('ApphighDirective', () => {
  it('should create an instance', () => {
    const directive = new ApphighDirective();
    expect(directive).toBeTruthy();
  });
});
